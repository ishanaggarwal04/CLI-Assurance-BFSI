# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# kane-cli run output (not source) — evidence, generated automation code,
# and run-time variables. CI publishes these to workflow artifacts + the
# test-results branch instead of committing them alongside the test.md files.
.testmuai/evidence/
.testmuai/tests/output-*/
.testmuai/variables/
