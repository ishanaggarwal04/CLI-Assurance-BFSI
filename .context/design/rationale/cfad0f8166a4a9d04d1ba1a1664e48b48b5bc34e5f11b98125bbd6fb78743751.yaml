anchor: ac-53
combinatorial:
  reason: because money applies, but no combinatorial input matrix was needed because this path is driven by the persona class and reachable loan surface.
  strength: 3
deferred_bindings: 0
expected_how: 'expected_mode: authored'
profile_family: unspecified
surface_state: absent
technique: Applied equivalence partitioning to the documented error_user persona because it represents a distinct wrong-loan-total outcome class; applied state/flow because the path may require navigation from the landing view to the first reachable loan-related banking view; applied negative/error because the state must remain distinguishable from generic failure. Risk gate implied strength=3 because money applies, but no combinatorial input matrix was needed because this path is driven by the persona class and reachable loan surface.
