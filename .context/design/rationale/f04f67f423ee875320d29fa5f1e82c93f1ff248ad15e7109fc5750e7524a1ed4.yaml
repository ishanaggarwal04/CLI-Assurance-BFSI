anchor: ac-29
combinatorial:
  reason: Applied auth/permission, negative, and state/flow techniques because the PRD states the frozen persona has an explicit 'no transfers' restriction and that this restriction must not be bypassable through ordinary UI actions. Risk-gated interaction strength was 3 because money, auth, and data-loss apply, but no covering array was needed because this scenario fixes a single persona-state path.
  strength: 2
deferred_bindings: 0
expected_how: 'expected_mode: authored'
profile_family: unspecified
surface_state: absent
technique: Applied auth/permission, negative, and state/flow techniques because the PRD states the frozen persona has an explicit 'no transfers' restriction and that this restriction must not be bypassable through ordinary UI actions. Risk-gated interaction strength was 3 because money, auth, and data-loss apply, but no covering array was needed because this scenario fixes a single persona-state path.
