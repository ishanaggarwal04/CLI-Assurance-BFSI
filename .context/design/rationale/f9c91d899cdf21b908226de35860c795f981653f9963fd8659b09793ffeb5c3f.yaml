anchor: ac-25
combinatorial:
  reason: Applied equivalence partitioning over the role/persona input and state/flow reasoning because the source promises a successful authentication transition into an administrative experience for admin_user. Used covering_array at risk-gated strength 3 because auth applies; it degraded to effective strength 1 because there is only one relevant input (persona), so the authoritative combo set enumerated persona=admin_user. The admin outcome is materially different from standard_user and therefore warrants its own scenario.
  strength: 2
deferred_bindings: 0
expected_how: 'expected_mode: authored'
profile_family: unspecified
surface_state: absent
technique: Applied equivalence partitioning over the role/persona input and state/flow reasoning because the source promises a successful authentication transition into an administrative experience for admin_user. Used covering_array at risk-gated strength 3 because auth applies; it degraded to effective strength 1 because there is only one relevant input (persona), so the authoritative combo set enumerated persona=admin_user. The admin outcome is materially different from standard_user and therefore warrants its own scenario.
